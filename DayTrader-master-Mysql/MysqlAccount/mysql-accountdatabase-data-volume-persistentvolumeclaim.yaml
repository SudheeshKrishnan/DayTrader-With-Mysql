apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: mysql-accountdatabase-data-volume
  name: mysql-accountdatabase-data-volume
  namespace: default
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
